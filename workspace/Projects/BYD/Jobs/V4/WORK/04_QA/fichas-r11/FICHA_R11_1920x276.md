# Ficha tecnica R11 — 1920x276

Estado: `PENDING_HEAD_REVIEW`. Esta ficha nao e QA visual, nao confirma execucao e nao aprova entrega.

- Linha de origem planejada: `BYD_TPL_1920x276.psd` R03, com a correcao R08 limitada ao condicional de parcelas do Dolphin Mini; a execucao R08 ainda deve ser comprovada por log, hash e revisao.
- Escopo R11: 20 ofertas neste formato; 140 PNGs somente quando somado aos seis formatos do `production_spec_r10.json`.
- Estrutura esperada para QA posterior: 20 estados em `BG`, `VEICULO`, `LEGAL`, `CONDICIONAIS` e `OFERTA`; `FIXO`, `#GUIAS`, 20 Layer Comps quando decodificaveis e um UUID de BG compartilhado.
- Conteudo esperado: texto editavel confrontado por IDs da fonte; parcelas Dolphin Mini com CR normalizado, wrap 15 e zona `[970,84,220,55]` centralizada proporcionalmente.
- Medidas nao observadas: legibilidade minima, geometria final, tempo, bytes, hash, presenca de PNG e aprovacao humana.

## Adendo nativo R12 (pendente)

`patch_controle_r12.jsx` substitui a linha planejada R08 para esta correcao: parcela do Dolphin Mini, a partir do R03, com CR normalizado, wrap 15 e a mesma zona. A ficha nao afirma que o patch executou. O montador R11 v2 pode selecionar somente o PSD/PNG R12 que exista apos um unico `OK|completed` sem `ERRO`; se o R12 falhar ou ficar incompleto, nenhum arquivo parcial R12 pode entrar na montagem.
