# Ficha tecnica R11 — 360x80

Estado: `PENDING_HEAD_REVIEW`. Derivada de `WORK/00_MATRIZ/production_spec_r10.json`; nao e QA visual, nao confirma execucao e nao aprova entrega.

- Escopo planejado: 20 ofertas; familia micro; zona tag `[8,23,56,12]`, badges `[8,38,77,12]`, award `[68,23,16,16]`, margem segura 4 px. Tagline, legal, detail e generic_headline sao omissoes previstas pelo spec.
- Estrutura esperada para QA posterior: 20 estados em `BG`, `VEICULO`, `LEGAL`, `CONDICIONAIS` e `OFERTA`; `FIXO`, `#GUIAS`, 20 Layer Comps quando decodificaveis e um UUID de BG compartilhado.
- Conteudo esperado: configuracao micro_dual declarada em 12 ofertas no spec r10; confrontar os IDs de titulo, preco, beneficio e headline com a fonte por whitespace normalizado.
- Visibilidade esperada: o condicional de parcelas do Dolphin Mini deve seguir a correcao R08 apenas se a execucao e o log a comprovarem; nao presumir aplicacao.
- Medidas nao observadas: legibilidade minima, geometria final, tempo, bytes, hash, presenca de PNG e aprovacao humana.

## Adendo nativo R12 (pendente)

Nenhuma correcao micro foi declarada no `patch_controle_r12.jsx`. A montagem R11 v2 conserva a fonte R10 e o preview original; isso nao confirma QA visual nem execucao R10.
